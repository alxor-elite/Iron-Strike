# Normalise line endings so the repo stays LF regardless of platform.
* text=auto eol=lf

# Keep these binary-safe if they are ever added under public/assets/.
*.png binary
*.jpg binary
*.webp binary
*.glb binary
*.gltf binary
*.ogg binary
*.mp3 binary
*.wav binary
